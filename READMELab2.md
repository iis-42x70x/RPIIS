# Лабоаторная работа 2

## Цели:
*1. Разработать библиотеку содержащую функции, выполняющие действия, указанные в
индивидуальном задании, на любом императивнойм языке
программирования (Pascal, C\C++, Java, C#, Python и др.)*

*2. Разработать тестовую программу, которая демонстрирует
работоспособность реализованной библиотеки работы со структурой
данных.*

*3. По результатам выполнения задания составить отчет.*

## Вариант: 
*1.Реализовать программу, формирующую множество равное объединению произвольного
количества исходных множеств (с учётом кратных вхождений элементов).*

## Основные понятия:
**Множество** — простейшая информационная конструкция и математическая структура, позволяющая рассматривать какие-то объекты как целое, связывая их. Объекты, связываемые некоторым множеством, называются элементами этого множества.

**Объединением неориентированных множеств A и B с учётом кратных вхождений элементов** будем называть неориентированное множество S тогда и только тогда, когда для любого x истинно S|x| = max{A|x|, B|x|}.

**Объединением неориентированных множеств A и B без учёта кратных вхождений элементов** будем называть неориентированное множество S тогда и только тогда, когда длялюбого x истинно S|x| = min{max{A|x|, B|x|}, 1}.

## Функции и алгоритмы операций:
*В программе я реализовала следующие функции: run_tectcase, CountMultiplicities, FindMaxMultiplicities, OutputResult.*

### 1. Функция по чтению из файла

Основная цель функции **run_tectcase** - прочитать данные из файла, который содержит описание наборов, и сохранить эти данные в структуре vector<vector<string>>. Файл имеет определенный формат, который предполагает наличие числа, указывающего количество наборов, за которым следуют строки, представляющие сами наборы. Каждый набор содержит элементы, которые могут быть либо простыми строками, либо вложенными наборами (представленными в фигурных скобках).

Сначала происходит открытие файла по указанному пути. Если файл не открылся, выводит сообщение об ошибке и завершает работу функции.
Читается первое число из файла и сохраняется его в переменную num_sets. Пропускаются оставшиеся символы в первой строке после числа. Изменяется размер вектора sets так, чтобы он мог вместить num_sets внутренних векторов (по одному для каждого набора).
Дальше происходит чтение и обработка каждого набора: цикл выполняется num_sets раз. Считывается вся следующая строка из файла в переменную line. Эта строка представляет собой один набор.Далее проходим по символам строки line, собирая символы в переменную element.
Затем обрабатываем элементы в скобках {}. Если встречается символ {, переходит в режим "внутри скобок". Если встречается символ } и был в режиме "внутри скобок":
Добавляет } к текущему element. 

Если element имеет размер 2 (например, {}), добавляет его как есть в текущий набор (sets[i]). Если element имеет размер больше 2 (например, {a b}):
Извлекает содержимое между скобками (a b). Разбивает это содержимое на отдельные элементы по пробелам (a, b). Сортирует эти элементы по алфавиту.
Формирует новую строку набора(element), объединяя отсортированные элементы обратно в формат {элемент1 элемент2 ...}.
Добавляет эту отсортированную строку набора(element) в текущий набор (sets[i]).
Очищает element.

Если встречается пробел и он не находится внутри скобок:
Если element не пуст (содержит собранные символы), добавляет его как отдельный элемент в текущий набор (sets[i]) и очищает element. Во всех остальных случаях 
добавляет символ к текущему element. После завершения итерации по строке, если element все еще содержит символы, добавляет его как последний элемент в текущий 
(sets[i]).
 В конце проиходит закрытие файла.

### 2. Функция по подсчёту кратности элементов
 
Алгоритм функции **CountMultiplicities** работает следующим образом: проходим по каждому внутреннему вектору строк (представляющему собой множество) во входном 
векторе векторов sets. Для каждого множества он создается два вспомогательных вектора: один для хранения уникальных элементов и другой для хранения их 
соответствующих счетчиков. Затем происходит итерация по элементам текущего множества.  Для каждого элемента проверяется, был ли он уже добавлен в вектор 
уникальных элементов. Если элемент найден, его счетчик увеличивается; если нет, элемент добавляется в вектор уникальных элементов, а его счетчик инициализируется 
единицей.  

После обработки всех элементов текущего множества, он создает вектор пар, где каждая пара содержит уникальный элемент и его подсчитанное количество появлений, и 
добавляет этот вектор пар в результирующий вектор counts на соответствующей позиции, после чего переходит к следующему множеству.  В конце функция возвращает 
результирующий вектор векторов пар, где для каждого исходного множества указаны его уникальные элементы и их количества.

### 3. Функция по поиску максимальных кратностей элементов

Алгоритм функции FindMaxMultiplicities проходит по каждому внутреннему вектору пар (представляющему собой подсчеты элементов в одном множестве) во входном векторе 
векторов counts. Для каждой пары (элемент, количество) проверяется, существует ли уже пара с таким же элементом в результирующем векторе max_counts. Если пара с 
таким элементом найдена, обновляется ее количество, устанавливая максимальное значение между текущим количеством в max_counts и количеством из обрабатываемой 
пары. Если пара с таким элементом не найдена, добавляем текущую пару в max_counts. В итоге функция возвращает вектор пар, где для каждого уникального элемента, 
встреченного во всех исходных множествах, указано его максимальное количество появлений среди всех множеств.

### 4. Функция по выводу результата объединения множеств:

Функция последовательно выводит элемент соответсвенно кратности указанной в паре с ним из вектора пар max_count.

## Система Google Test:

Первые два теста проверяют работу функции **run_testcase**, сравнивая ожидаемый результат и результат работы функций
```
TEST(RunTestcaseTest, SubsetPermutation) {
    int num_sets = 0;
    vector<vector<string>> sets;
    run_testcase("permutation.txt", num_sets, sets);
    vector<vector<string>> expected_sets = { {"{a b}", "{a b}"}, {"{a b}"} };
    ASSERT_EQ(num_sets, expected_sets.size());

    for (int i = 0; i < num_sets; ++i) {
        ASSERT_EQ(sets[i], expected_sets[i]);
    }
}

TEST(RunTestcaseTest, FileNotFoundTest) {
    int num_sets = 0;
    vector<vector<string>> sets;
    run_testcase("non_existent_file.txt", num_sets, sets);
    ASSERT_EQ(num_sets, 0);
    ASSERT_TRUE(sets.empty());
}
```

Следующие два теста проверяют работу функции **CountMultiplicities**:

```
TEST(CountMultiplicitiesTest, SingleSetTest) {
    vector<vector<string>> sets = { {"aa", "b", "b", "{}"} };
    vector<vector<pair<string, int>>> expected_counts = { {{"aa", 1}, {"b", 2}, {"{}", 1}} };
    ASSERT_EQ(CountMultiplicities(sets), expected_counts);
}

TEST(CountMultiplicitiesTest, MultipleSetsTest) {
    vector<vector<string>> sets = { {"a", "b", "b", "c"}, {"x", "y", "z", "x", "y"} };
    vector<vector<pair<string, int>>> expected_counts = { {{"a", 1}, {"b", 2}, {"c", 1}}, {{"x", 2}, {"y", 2}, {"z", 1}} };
    ASSERT_EQ(CountMultiplicities(sets), expected_counts);
}
```

Следующие три теста для функции **FindMaxMultiplicities**:

```
TEST(FindMaxMultiplicitiesTest, SingleSetTest) {
    vector<vector<pair<string, int>>> counts = { {{"a", 1}, {"b", 2}, {"c", 1}} };
    vector<pair<string, int>> expected_max_counts = { {"a", 1}, {"b", 2}, {"c", 1} };
    ASSERT_EQ(FindMaxMultiplicities(counts), expected_max_counts);
}

TEST(FindMaxMultiplicitiesTest, MultipleSets) {
    vector<vector<pair<string, int>>> counts = {
        {{"a", 1}, {"b", 2}, {"c", 3}},
        {{"a", 2}, {"b", 4}, {"d", 5}},
        {{"b", 3}, {"c", 1}, {"e", 2}}
    };
    vector<pair<string, int>> expected = { {"a", 2}, {"b", 4}, {"c", 3}, {"d", 5}, {"e", 2} };
    vector<pair<string, int>> result = FindMaxMultiplicities(counts);
    ASSERT_EQ(result, expected);
}

TEST(FindMaxMultiplicitiesTest, MultipleSetsTest) {
    vector<vector<pair<string, int>>> counts = { {{"a", 1}, {"b", 2}, {"c", 1}}, {{"x", 2}, {"y", 2}, {"z", 1}} };
    vector<pair<string, int>> expected_max_counts = { {"a", 1}, {"b", 2}, {"c", 1}, {"x", 2}, {"y", 2}, {"z", 1} };
    ASSERT_EQ(FindMaxMultiplicities(counts), expected_max_counts);
}
```

Последний тест проверяет корректность вывода результата:

```
TEST(OutputResultTest, SingleSetTest) {
    vector<pair<string, int>> max_counts = { {"a", 1}, {"b", 2}, {"c", 1} };
    testing::internal::CaptureStdout();
    OutputResult(max_counts);
    string output = testing::internal::GetCapturedStdout();
    ASSERT_EQ(output, "The union of the sets: \na b b c \n");
}
```

![Снимок экрана 2025-05-02 202328](https://github.com/user-attachments/assets/bfcebb02-5cd4-458d-85c5-a2e2a3336323)
![Снимок экрана 2025-05-02 202306](https://github.com/user-attachments/assets/b8d75a1b-b9fe-45fd-b1d6-05e1f889962e)


## Выводы:
В результате выполнения лабораторной работы были получены знания из теории множеств, создана программа, реализующая операции на множествах, подключена система Google Test для проверки работы программы

## Используемые источники:
Объединение множеств [[https://dementiy.gitbooks.io/algo/content/heaps.html](https://medium.com/nuances-of-programming/объединение-множеств-c-практическое-руководство-с-реальными-примерами-c4c74794f06e)]

Тесты [[(https://ru.wikipedia.org/wiki/Очередь_с_приоритетом_(программирование))](https://vitalissius.github.io/GoogleTest+VisualStudio/)]
