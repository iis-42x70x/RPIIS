# Работа с двунаправленным списком

## Цель:

Целью оздания данной библиотеки является создание и оптимальная работа с двунаправленными списками и их элементами, включающая следующие операции:
- создание списка
- удаление элементов из списка
- вставка элементов в список
- сортировка элементов списка
- операция объединения двух списков
- операция пересечения двух списков
- генерация случайных элементов из диапазона
- поиск элемента в списке

  
## Описание алгоритмов

- Вставка **insert** - вставка элемента в двунаправленный список, значение которого указывает пользователь
- Вставка в место в списке **insert_mesto** - вставка элемента в определённое место, указанное пользователем с помощью введенного целочисленного значения
- Удаление элемента **deleting** - удаление элемента\элементов(если значения присутствуют не единоразово) из списка, значение которого указывает пользователь
- Генерация списка **generateRandomlist** - создаёт список и заполняет случайными элементами
- Сортировка **Sort** - сортировка списка по возрастанию
- Поиск элемента в списке **search** - определяет наличие того или иного элемента в списке и возвращает **ture**, если он есть и **false** в обратном случае
- Операция объединения **merge** - находит значение объедиенение двух списков, принимая их за множества, то есть элементы конечного списка не имеют повторных вхождений
- Операция пересечения **per** - находит значение пересечение двух списков, используя тот же концепт

### Так же используются и другие функции, например, **menu()** и **vybor()** для взаимодействия с пользователем и для создания интерфейса основной программы
  

##  Файлы проекта

- main.cpp - Основной файл с консольным приложением.
- Header.h - Заголовочный файл со структурой двунаправленного списка.
- func.cpp - Файл с реализацией функций работы с двунаправленными списками.

## 🛠️ Тестирование
Небольшая программа с реализацией моей библиотеки:
```cpp
#include <iostream>
#include "D:\учёба\пивас\dn_spisok\лаба_1\Header.h"

using namespace std;


int main()
{
    Node* list1 = nullptr;
    Node* list2 = nullptr;
    while (true)
    {
        menu();
        int k;
        cin >> k;
        vybor(k, &list1, &list2);
    }
}

```
## Пример работы:

<img src="https://ltdfoto.ru/images/2025/02/26/C1F6F177-25C5-4BAE-8E9D-129D2A3EEF18.png" alt="C1F6F177-25C5-4BAE-8E9D-129D2A3EEF18.png" border="0" />

Как можно заметить, пользователь видит "меню" и выбирает один из пуктов - комбинация цифр для выполнения того или иного действия. После выполнения задачи, программа снова демонстрирует меню и пользователь снова может дать команду. Программа прекращет работу при вводе "0" в консоль.

## Ещё пример:

<img src="https://ltdfoto.ru/images/2025/02/26/810EE229-D400-4498-9215-0B27194E97CD.png" alt="810EE229-D400-4498-9215-0B27194E97CD.png" border="0" />

Программа (по запросу) сначала генерирует 2 списка, а затем показывает объединение.

## Пример_ещё:

Программе поступает запрос на сортировку списка, а затем на удаление элемента из него.

<img src="https://ltdfoto.ru/images/2025/02/26/C1544C12-A66B-48AE-818E-424D5507D3F1.png" alt="C1544C12-A66B-48AE-818E-424D5507D3F1.png" border="0" />

<img src="https://ltdfoto.ru/images/2025/02/26/IZOBRAZENIE21a17db6e2cb69bd.png" alt="IZOBRAZENIE21a17db6e2cb69bd.png" border="0" />




## Заключение

Отчёт демонстрирует исправную работу функций из созданной и успешно подключённой статической библиотеки. Алгоритмы, представленные к реализации в условии задачи, успешно созданы на языке программирования С++.
## Использованные источники:

### Свободная энциклопедия "Википедия"
- https://ru.m.wikipedia.org/wiki/

### Habr:
- https://habr.com/ru/articles/717572/
